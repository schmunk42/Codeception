class_name: OtherGuy
modules:
    enabled: [PhpBrowser, Filesystem, OtherHelper]
    config:
        PhpBrowser:
            url: http://web2:80

env:
    default:

    webdriver:
        modules:
            enabled: [WebDriver, Filesystem, OtherHelper]
            config:
                WebDriver:
                    url: http://web2:80
                    browser: firefox



coverage:
    enabled: true
    remote: false
    include:
        - '/../app/*'